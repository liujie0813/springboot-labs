server:
  port: 18761

spring:
  application:
    name: eureka-server

eureka:
  instance:
    hostname: eureka-node1
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://eureka-node2:28761/eureka/