server:
  port: 8761

spring:
  application:
    name: eureka-server-standalone

eureka:
  client:
    register-with-eureka: false # 是否注册到 Eureka Server，默认为 true
    fetch-registry: false # 是否从 Eureka Server 获取注册表，默认为 true